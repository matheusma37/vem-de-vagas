<h1>Vem de Vagas</h1>

<h2>Empresas</h2>
<ul>
  <% @companies.each do |company| %>
    <li><%= link_to company.name, company %></li>
  <% end %>
</ul>

<h2>Vagas</h2>
<% @job_opportunities.each do |job_opportunity| %>
  <div>
    <%= link_to job_opportunity.title, job_opportunity %>
    <p><%= "Vagas disponíveis: #{job_opportunity.total_job_opportunities}" %></p>
    <p>Nível: <%= t(job_opportunity.professional_level) %></p>
  </div>
<% end %>